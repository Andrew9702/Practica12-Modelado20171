//incluye la biblioteca que permite imprimir en pantalla
#include <stdio.h>

//Metodo principal para mandar a llamar a la funcion print de stdio.h
int main(){
	//Se manda a llamar a la funcion print con el arreglo de caracteres "hola mundo"
	printf("¡Hola, mundo!");
	//Se regresa  por defecto, esto indica que no hubo ningun error durante la ejecucion
	return 0;
}
